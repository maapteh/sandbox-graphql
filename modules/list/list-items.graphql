query listItems($id: Int!) {
    list(id: $id) {
        id
        items {
            ... on ListItemProduct {
                id
                description
                quantity
            }
            ... on ListItemRecipe {
                id
                title
                description
                quantity
            }
        }
    }
}
