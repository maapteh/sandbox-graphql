query covidHistorical($size: Int, $country: String) {
    covidHistorical(size: $size, country: $country) {
        dates
        results {
            province
            deaths
            cases
        }
    }
}
