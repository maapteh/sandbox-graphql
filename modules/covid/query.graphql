query covidHistorical($days: Int, $country: String) {
    covidHistorical(days: $days, country: $country) {
        dates
        results {
            province
            deaths
            cases
        }
    }
}
